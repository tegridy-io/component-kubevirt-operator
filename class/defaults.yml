parameters:
  kubevirt_operator:
    =_metadata: {}

    namespace: syn-kubevirt-operator

    manifestsVersion: v1.1.1

    operator:
      replicas: 2

    instancePrefix: syn-kubevirt
    instances: {}
